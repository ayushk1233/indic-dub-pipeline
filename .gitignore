# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Virtual environments
venv/
.venv/

# Environment
.env
.env.*

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Logs
*.log

# Model checkpoints
models/
checkpoints/
*.ckpt
*.pt
*.pth
*.bin
*.onnx

# Audio / Video outputs
outputs/
artifacts/
*.wav
# Except the recording-session fixtures. Colab reaches them with `git pull`,
# and 8 MB of derived audio is worth carrying to avoid re-uploading 200 MB of
# video into every fresh runtime. The .mov sources stay ignored.
!fixtures/*.wav

# The per-sentence slices, for the same reason and on the same budget — 1.9 MB.
# This needs its own line because the pattern above does not reach into a
# subdirectory. They are his real English, so they are the Whisper content
# floor, the same-language identity ceiling, and what every generated clip is
# listened to against; a GPU session without them can report numbers but
# cannot calibrate them.
!fixtures/en_speaker/*.wav

# And the Hindi ones, which is its own line for the same reason and was
# missed once. Measured 2026-09-19: slots.json is not audio, so it reached
# Kaggle while the seven wavs did not. floor_rows() found the directory,
# found no clips, returned an empty list, and the report printed a verdict
# under `floor (him) cer -` — a synthesis CER read against zero, which is
# the one thing that probe exists to prevent. A gitignore rule cost a GPU
# session's worth of uncalibratable numbers.
!fixtures/hi_speaker/*.wav
*.mp3
*.flac
*.aac
*.m4a
*.mp4
*.mov
*.mkv

# Databases
*.db
*.sqlite
*.sqlite3

# Coverage
.coverage
.pytest_cache/
htmlcov/

# Misc
*.tmp
finetune_plan/
finetune_plan
plan.md
progress.md
transliteration/
transliteration
FINETUNE_PLAN.md
TRANSLITERATION_PLAN.md
PROGRESS.md
# Research notes pulled in from outside this repo. Deliberately never
# committed — they were swept into a commit once by `git add -A` and had to
# be removed by rewriting history before the branch was pushed. Ignoring them
# is what makes that impossible rather than merely avoided.
compass_artifact_*.md
